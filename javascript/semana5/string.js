function stringSplit() {
    const fullname = prompt("Nombre y apellido: ");
    // const fullname_fixed = fullname.replace();
    const array_fullname = fullname.split(" ");
    console.log(array_fullname);
}